/**
 * ========================================
 *   🔐 本地配置文件示例
 *   复制为 config.local.js 并填入你的密钥
 *   config.local.js 已被 .gitignore 忽略
 * ========================================
 */
const config = {
  deepseek: {
    apiKey: 'sk-你的DeepSeek密钥',
  },
  push: {
    pushdeer: {
      key: 'PDU你的PushDeer密钥',
    }
  }
};
export default config;
